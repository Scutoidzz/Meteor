import PyQt6.QtWidgets as QtWidgets
import PyQt6.QtCore as QtCore
import PyQt6.QtGui as QtGui
import sys

def what():
    window = QtWidgets.QWidget()
    layout = QtWidget.QVBoxLayout()
    whatdoyouwant = QtWidget.QLabel()
    layout.AddWidget(whatdoyouwantd)
    buttons = QtWidgets.QHBoxLayout()
    client = QtWidgets.QPushButton("Client"); buttons.addWidget(client)
    server = QtWidgets.QPushButton("Server"); buttons.addWidgets(server)
    layout.addLayout(buttons)
    window.setLayout(layout)
 
if __name__ == "__main__":
    app = QtWidgets.QApplication(sys.argv)
    what()  cgghbbbgvvfgcv  